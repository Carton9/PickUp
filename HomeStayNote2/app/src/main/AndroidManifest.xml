<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.qazwq.homestaynote2">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.SEND_SMS" />
    <uses-permission android:name="android.permission.RECEIVE_SMS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainPanel"
            android:label="@string/title_activity_main_panel"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />

        <service
            android:name=".MyService"
            android:enabled="true"
            android:exported="true"
            android:process=":remote" />

        <activity android:name=".StudentSignIn" />
        <activity android:name=".StudentList" />
        <activity android:name=".EventSetting" />
        <activity android:name=".EventList" />

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyDq-ClyOQ2BTv-ok-dYbiFafe5aAtHKJ5Y" />

        <activity
            android:name=".PickUpMode"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:label="@string/title_activity_pick_up_mode"
            android:theme="@style/FullscreenTheme"></activity>
    </application>

</manifest>